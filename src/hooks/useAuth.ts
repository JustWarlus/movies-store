import { useAppSelector } from "store/hooks/hooks";

// export const useAuth = () => {
//   const { email, name } = useAppSelector((state) => state.name);

//   return {
//     isAuth: !!name,
//     email,
//     name,
//   };
// };
